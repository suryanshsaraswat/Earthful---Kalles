<style>
  .timeline ul{padding:50px 0}
.timeline ul li{list-style-type:none;position:relative;width:2px;margin:0 auto;padding-top:30px;background:#9F95A0}
.page_106157244600 main#MainContent{background:#FFF8F3}
.time-line-content p{margin-top:7px;margin-bottom:0;font-size:14px;color:#000}
.time-line-content h2{margin:0;margin-bottom:-6px;font-size:14px;font-weight:600;color:#212427}
.time-line-content{border:1px solid #9F95A0;border-radius:15px;opacity:0;transition:opacity .5s ease-in-out}
.timeline ul li::after{content:"";position:absolute;left:50%;bottom:65px;transform:translateX(-50%);width:12px;height:12px;border-radius:50%;background:#9F95A0;z-index:1;transition:.2s}
.timeline ul li div{position:relative;bottom:24px;width:400px;padding:15px}
.timeline ul li:nth-child(odd) div{left:45px}
.timeline ul li:nth-child(even) div{left:-439px}
.timeline ul li:nth-child(even) div::before{right:-15px;border-width:8px 0 8px 16px;border-color:transparent transparent transparent #f45b69}
time{display:block;font-size:1.2rem;font-weight:700;margin-bottom:8px}
.timeline ul li::after{transition:background .5s ease-in-out}
.timeline ul li div{visibility:hidden;opacity:0;transition:all .5s ease-in-out}
.timeline ul li.in-view div{transform:none;visibility:visible;opacity:1}
.timeline-heading h2{text-align:center;font-size:35px;position:relative}
@media screen and (max-width: 900px) {
.timeline ul li div{width:250px}
.timeline ul li:nth-child(even) div{left:-289px}
}
@media screen and (max-width: 600px) {
.timeline ul li{margin-left:20px}
.timeline ul li div{width:calc(100vw - 91px)}
.timeline ul li:nth-child(even) div{left:45px}
.timeline ul li:nth-child(even) div::before{left:-15px;border-width:8px 16px 8px 0;border-color:transparent #f45b69 transparent transparent}
}
@media(max-width:450px) {
.ninety{text-align:left!important;margin-left:10px}
.zero{text-align:left!important;margin-left:10px}
.timeline-heading h2{text-align:left!important;font-size:20px!important;padding-left:10px!important}
.timeline-heading h2::after{content:"";position:absolute;width:215px;height:3px;top:15px;right:0;background-image:url(https://cdn.shopify.com/s/files/1/0600/8356/8824/files/Line_1_1.png?v=1693978893);background-repeat:no-repeat;background-size:100%}
.timeline ul{padding:13px 0}
}
.timeline-clippy ul li::after{width:40px;height:40px;border-radius:0}
.timeline-rhombus ul li::after{clip-path:polygon(50% 0%,100% 50%,50% 100%,0% 50%)}
.timeline-rhombus ul li div::before{bottom:12px}
.timeline-star ul li::after{clip-path:polygon(50% 0%,61% 35%,98% 35%,68% 57%,79% 91%,50% 70%,21% 91%,32% 57%,2% 35%,39% 35%)}
.timeline-heptagon ul li::after{clip-path:polygon(50% 0%,90% 20%,100% 60%,75% 100%,25% 100%,0% 60%,10% 20%)}
.timeline-infinite ul li::after{animation:scaleAnimation 2s infinite}
.ninety{text-align:center;margin-top:10px}
.zero{text-align:center;margin-bottom:10px}
  
</style>
<div class="timeline-heading">
  <h2>{{ section.settings.timeline-heading }}</h2>
</div>
<section class="timeline">
  <ul>
    <div class="zero">0 Days</div>
    {% for block in section.blocks %}
    <li>
      <div class="time-line-content">
        <h2>{{ block.settings.block_heading }}</h2>
        <p>{{ block.settings.block_text }}</p>
      </div>
    </li>
      {% endfor %}
      <div class="ninety">90 Days</div>
  </ul>
</section>
{% comment %}<script>
  (function () {
  "use strict";
  var items = document.querySelectorAll(".timeline li");
  function isElementInViewport(el) {
    var rect = el.getBoundingClientRect();
    return (
      rect.top >= 0 &&
      rect.left >= 0 &&
      rect.bottom <=
        (window.innerHeight || document.documentElement.clientHeight) &&
      rect.right <= (window.innerWidth || document.documentElement.clientWidth)
    );
  }

  function callbackFunc() {
    for (var i = 0; i < items.length; i++) {
      if (isElementInViewport(items[i])) {
        items[i].classList.add("in-view");
      }
    }
  }

  // listen for events
  window.addEventListener("load", callbackFunc);
  window.addEventListener("resize", callbackFunc);
  window.addEventListener("scroll", callbackFunc);
})();

</script>{% endcomment %}
<script>
  (function () {
    "use strict";
    var items = document.querySelectorAll(".timeline li");

    function isElementInViewport(el) {
      var rect = el.getBoundingClientRect();
      return (
        rect.top >= 0 &&
        rect.left >= 0 &&
        rect.bottom <=
          (window.innerHeight || document.documentElement.clientHeight) &&
        rect.right <=
          (window.innerWidth || document.documentElement.clientWidth)
      );
    }

    function callbackFunc() {
      for (var i = 0; i < items.length; i++) {
        if (isElementInViewport(items[i])) {
          items[i].classList.add("in-view");
        }
      }
    }

    function fadeInTimelineContent() {
      var timelineContent = document.querySelectorAll(".time-line-content");
      for (var i = 0; i < timelineContent.length; i++) {
        if (isElementInViewport(timelineContent[i])) {
          timelineContent[i].style.opacity = 1;
          
        } else {
          timelineContent[i].style.opacity = 0.5; // Fade out if not in view
        }
      }
    }

    // Listen for events
    window.addEventListener("load", callbackFunc);
    window.addEventListener("resize", callbackFunc);
    window.addEventListener("scroll", callbackFunc);
    window.addEventListener("scroll", fadeInTimelineContent);
  })();
</script>
{% schema %}
  {
    "name": "Timeline",
    "settings": [
      {
        "type":"text",
        "id":"timeline-heading",
        "label":"Enter timeline heading",
        "default":"How it Works?"
      }
    ],
    "blocks":[
      {
        "name":"Timeline block",
        "type":"page",
        "settings":[
          {
            "type":"text",
            "id":"block_heading",
            "label":"Enter time line block heading"
          },
          {
            "type":"text",
            "id":"block_text",
            "label":"Enter timeline paragraph"
          }
        ]
      }
    ],
    "presets":[
      {
        "name":"Timeline",
        "category":"page"
      }
    ]
  }
{% endschema %}